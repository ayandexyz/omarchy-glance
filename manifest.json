{
  "schemaVersion": 1,
  "id": "io.github.ayan-de.glance",
  "name": "Glance Face Unlock",
  "version": "0.1.3",
  "author": "Ayan De",
  "license": "MIT",
  "description": "Face unlock with liveness detection: armed state, enrolled identities, the last scan's verdict, and a test scan, in a native Omarchy bar panel.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Glance Face Unlock",
    "description": "One bar icon and one panel for the glanced face-unlock daemon: whether it is armed, who is enrolled, what the last scan decided and why, plus arm, disarm and test-scan actions.",
    "category": "System",
    "aliases": [
      "glance",
      "face-unlock"
    ],
    "defaultSection": "right",
    "allowMultiple": false,
    "defaults": {
      "refreshIntervalSec": 30,
      "glancectlPath": ""
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Refresh interval (seconds)",
        "description": "How often the daemon's status is polled while idle. The panel refreshes on open and after every action regardless.",
        "min": 5,
        "max": 600,
        "step": 5,
        "defaultValue": 30
      },
      {
        "key": "glancectlPath",
        "type": "string",
        "label": "glancectl path",
        "defaultValue": "",
        "description": "Optional. Absolute path to glancectl; empty means /usr/bin/glancectl from the glanced package. It is checked before every run: no symlinks, every directory and the file owned by you or root and writable by nobody else, the file regular and executable, and the same file (device, inode, size, mtime) the previous run was checked against. For a source checkout give the venv binary in full, e.g. /home/you/glance-linux/.venv/bin/glancectl."
      }
    ]
  }
}
